<div class="container">
    <div class="row">
      <% @calendars.each do |calendar| %>
      <div class="col-md-6 p-b-50">
							<!-- Block2 -->
							<div class="block2">
								<div class="block2-img wrap-pic-w of-hidden pos-relative">
									<%# = image_tag (calendar.img_url) %>

									<div class="block2-overlay trans-0-4">
										<div class="block2-btn-addcart w-size1 trans-0-4">
											<!-- Button -->
												<a href="/calendar/<%= calendar.release_link %>" class="btn btn-primary btn-seva w-100">Voir la sneaker</a>
										</div>
									</div>
								</div>

								<div class="block2-txt p-t-20">
									  <p class="text-center"><a href="/calendar/<%= calendar.id %>">
                                        <%= calendar.sneaker_id %> - 
                                        <%= calendar.release_date.strftime("%d/%m/%Y à %I:%M%p") %></a>
                                      </p>
								</div>
					</div>
			</div>
			<% end %>
    </div>
</div>